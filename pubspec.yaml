name: work_time
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.1.0+2

environment:
  sdk: '>=3.0.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  intl: ^0.18.1
  shared_preferences: ^2.1.1
  flexible_scrollable_table_view: ^2.10.0
  april_flutter_screen_adapter: ^1.1.1
  extended_value_notifier: ^0.0.1

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true

